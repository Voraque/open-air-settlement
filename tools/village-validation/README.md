Test-only Fabric1.21.1 intermediary audit. Register VillageAudit as a server entrypoint only in a disposable fixture. Compile against its server-intermediary.jar, Fabric lifecycle API and libraries with Java21. Tests call actual villager level-up/restock and NBT paths. OAS_WORKSTATION_PROTECTION_VERIFIED=false is a documented upstream limitation, not an assertion success. Never copy this test jar to a player server. See the September7 book-first decision for the acceptance boundary.
